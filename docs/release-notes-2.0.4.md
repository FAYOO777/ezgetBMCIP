# ezgetBMCIP 2.0.4（本地支线）

- 地址验证结果按实际 Ping、HTTPS 和 HTTP 探测显示。
- 仅 443 使用 HTTPS，只有 80 使用 HTTP，两者都通时默认 HTTPS。
- 仅 Ping 或三项无响应时不生成管理页面 URL，只提供重新检测和复制地址。
- 运行页不再显示“查看诊断详情”；完整证据仍写入日志和支持包。
- 不改变 DHCP、并行探测、浏览器请求、历史重试或网卡恢复流程。
